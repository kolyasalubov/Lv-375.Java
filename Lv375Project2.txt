1. JDBC SQL (DDL+DML); no OOP
   connection

2. ORM 1 = 1
   Hibernate XML, annotation (Hibernate or JPA)
   Hibernate  JDBC+Dialect => SQL Request
   - methods
   - critearia
   - HQL
  session (session factory), hibernate.cfg.xml

3. JPA
   - methods (JPA, ...)
   - jpa named query
   - JPQL
   - Sprinda data JPA + repo... (+JPQL)
   entity manager	entity.xml (spring-data.xml, annotation)
